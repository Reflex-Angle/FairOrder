#pragma once
#include <cstdint>

using Price     = int64_t;   // integer ticks only
using Qty       = int64_t;
using OrderID   = uint64_t;
using TimeNs    = uint64_t;  // nanoseconds
using BatchID   = uint64_t;